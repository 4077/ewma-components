<div class="{__NODE_ID__}">

    <div class="components">
        <!-- component -->
        <div class="component {SELECTED_CLASS}" draggable="component" component_id="{ID}" title="{ID}" hover="hover">
            <div class="buttons" hover="hover">
                {RENAME_BUTTON}
                {DUPLICATE_BUTTON}
                {DELETE_BUTTON}
            </div>
            <div class="name">
                {NAME}
            </div>
            <div class="cb"></div>
        </div>
        <!-- / -->
    </div>

    {CREATE_BUTTON}
    <div class="cb"></div>

</div>
