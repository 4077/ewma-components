<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="instances">
        <!-- instance -->
        <div class="instance">
            <div class="cp">
                <div class="buttons">
                    {DELETE_BUTTON}
                </div>

                <div class="name">
                    {NAME}
                </div>
            </div>

            <div class="handler">
                {HANDLER}
            </div>
        </div>
        <!-- / -->
    </div>

    {CREATE_INSTANCE_BUTTON}
    <div class="cb"></div>

</div>
